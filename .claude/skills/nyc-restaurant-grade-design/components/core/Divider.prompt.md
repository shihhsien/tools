A hairline separator, optionally with the app's centered uppercase eyebrow label.

```jsx
<Divider label="or paste a link" />
<Divider />   {/* plain 1px rule */}
```

Notes:
- The label is auto-uppercased via CSS with `0.06em` tracking; pass it in sentence case.
